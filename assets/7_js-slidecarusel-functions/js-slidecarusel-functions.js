
$(document).ready(function() {
	
		/*var 
		flag = true,
		timerDuration = 3000,
		timer = 0;

		

			setInterval(function(){
				var
					slides = $('.homeSlider__slider-img'),
					activeSlide = slides.filter('.active'),
					nextSlide = activeSlide.next(),
					firstSlide = slides.first();

				/*var activeSlide = $('.content-block.active');*/
   					activeSlide.removeClass('active');

				if (nextSlide(slides)) {

    				nextSlide(slides).addClass('active');
    			} else {
    				firstSlide(slides).addClass('active');
    			}

			}, 1000);*/
		 
}); // вызываем написанный ранее модуль